---
title: "flr"
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  message = FALSE, 
  warning = FALSE,
  collapse = TRUE,
  error = TRUE,
  comment = "#>"
)
```

```{r}
library(FLCore)
library(ggplotFL)
library(tidyverse)
library(fishvice)
```

```{r}
rbx <- fishvice::mup_rbx("/u2/reikn/Tac/2022/01-cod/ass/mup/smx")

icod <- rbx %>% fishvice::rbx_to_flstock(pf = 1, pm = 1, fage = c(5, 10))
icod %>% plot()
```

